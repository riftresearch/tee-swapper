# ===========================================
# TEE Swapper Environment Configuration
# ===========================================

# -------------------------------------------
# PRODUCTION - Required
# -------------------------------------------

# PostgreSQL database connection string
DATABASE_URL=postgres://user:password@localhost:5432/tee_swapper

# -------------------------------------------
# PRODUCTION - Optional (have defaults)
# -------------------------------------------

# Server port (default: 3000)
PORT=3000

# Ethereum Mainnet RPC URL (default: https://eth.drpc.org)
ETH_RPC_URL=https://eth.drpc.org

# Base RPC URL (default: https://base.drpc.org)
BASE_RPC_URL=https://base.drpc.org

# -------------------------------------------
# E2E TESTS - Required for running e2e tests
# -------------------------------------------

# Private key of a funded wallet on Base chain for e2e tests
# Must have: ETH (for gas + test swaps), USDC, USDT
# Test amounts: 0.003 ETH, 9 USDC, 9 USDT
# WARNING: Use a dedicated test wallet, not a production wallet!
TEST_PRIVATE_KEY=0x...
